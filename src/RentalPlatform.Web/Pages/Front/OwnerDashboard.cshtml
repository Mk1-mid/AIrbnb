@page
@model RentalPlatform.Web.Pages.Front.OwnerDashboardModel
@{
    ViewData["Title"] = "Owner Dashboard - LuxeStay";
    ViewData["HtmlLang"] = "en";
    ViewData["HtmlClass"] = "dark";
    ViewData["BodyClass"] = "bg-background text-on-background min-h-screen flex flex-col antialiased";
    ViewData["FrontNavVariant"] = "owner";
    ViewData["FrontActiveNav"] = "dashboard";
}

@section Head {
<script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
<style>
    .glass-panel {
        background: rgba(11, 27, 43, 0.4);
        backdrop-filter: blur(20px);
        -webkit-backdrop-filter: blur(20px);
        border-top: 1px solid rgba(242, 202, 80, 0.2);
        border-left: 1px solid rgba(242, 202, 80, 0.05);
    }
    .ambient-shadow-lvl1 {
        box-shadow: 0 1px 3px rgba(15, 23, 42, 0.05);
    }
</style>
}

<main class="relative flex-1 md:ml-64 p-margin-mobile md:p-margin-desktop max-w-[1600px] w-full mx-auto flex flex-col gap-stack-lg min-h-screen">
    <div class="absolute inset-0 -z-10 bg-gradient-to-b from-[#0B1B2B] via-background to-background"></div>
    <div class="absolute top-0 right-0 -z-10 h-96 w-96 rounded-full bg-primary/5 blur-2xl"></div>

    <header class="flex flex-col md:flex-row justify-between items-start md:items-end gap-stack-md">
        <div>
            <span class="text-label-sm font-label-sm uppercase tracking-widest text-primary">Owner Suite</span>
            <h1 class="text-headline-lg-mobile md:text-headline-lg font-headline-lg-mobile md:font-headline-lg text-on-surface">Dashboard de Rendimiento</h1>
            <p class="text-body-md font-body-md text-on-surface-variant mt-1">Overview of your properties' performance.</p>
        </div>
        <div class="flex flex-wrap items-center gap-stack-sm w-full md:w-auto">
            <div class="relative min-w-[200px] flex-1 md:flex-none">
                <span class="material-symbols-outlined absolute left-3 top-1/2 -translate-y-1/2 text-on-surface-variant text-[20px]">home_work</span>
                <select class="w-full pl-10 pr-8 py-2 bg-surface-container-lowest/80 border border-outline-variant/40 rounded-lg text-label-md font-label-md text-on-surface focus:outline-none focus:ring-1 focus:ring-amber-500/50 focus:border-primary appearance-none cursor-pointer transition-all duration-300">
                    <option>Todas las propiedades</option>
                </select>
                <span class="material-symbols-outlined absolute right-3 top-1/2 -translate-y-1/2 text-on-surface-variant text-[20px] pointer-events-none">arrow_drop_down</span>
            </div>
            <div class="flex bg-surface-container-low rounded-lg p-1 border border-outline-variant/40">
                <button class="px-4 py-1.5 rounded-md text-label-sm font-label-sm text-on-surface-variant hover:text-primary hover:bg-surface-container-lowest/40 transition-all duration-300" type="button">Week</button>
                <button class="px-4 py-1.5 rounded-md text-label-sm font-label-sm bg-surface-container-lowest text-primary shadow-sm ambient-shadow-lvl1">Month</button>
                <button class="px-4 py-1.5 rounded-md text-label-sm font-label-sm text-on-surface-variant hover:text-primary hover:bg-surface-container-lowest/40 transition-all duration-300" type="button">Year</button>
            </div>
        </div>
    </header>

    <section class="grid grid-cols-1 md:grid-cols-3 gap-gutter">
        <div class="glass-panel rounded-lg p-stack-md ambient-shadow-lvl1 flex flex-col justify-between group hover:border-outline transition-all duration-300">
            <div class="flex justify-between items-start mb-stack-md">
                <div>
                    <p class="text-label-md font-label-md text-on-surface-variant">Ingresos totales</p>
                    <h3 class="text-headline-md font-headline-md text-primary mt-1">@(Model.Dashboard?.TotalRevenue.ToString("C0") ?? "$0")</h3>
                </div>
                <div class="w-10 h-10 rounded-full bg-surface-container flex items-center justify-center text-primary group-hover:bg-primary-fixed transition-all duration-300">
                    <span class="material-symbols-outlined text-[20px]">payments</span>
                </div>
            </div>
            <div class="flex items-center gap-2">
                <span class="flex items-center text-label-sm font-label-sm text-[#0f7a0f] bg-[#e6f4e6] px-2 py-0.5 rounded">
                    <span class="material-symbols-outlined text-[14px] mr-1">trending_up</span> +12.5%
                </span>
                <span class="text-label-sm font-label-sm text-on-surface-variant">vs last month</span>
            </div>
        </div>
        <div class="glass-panel rounded-lg p-stack-md ambient-shadow-lvl1 flex flex-col justify-between group hover:border-outline transition-all duration-300">
            <div class="flex justify-between items-start mb-stack-md">
                <div>
                    <p class="text-label-md font-label-md text-on-surface-variant">Reservas totales</p>
                    <h3 class="text-headline-md font-headline-md text-primary mt-1">@(Model.Dashboard?.TotalBookings ?? 0)</h3>
                </div>
                <div class="w-10 h-10 rounded-full bg-surface-container flex items-center justify-center text-primary group-hover:bg-primary-fixed transition-all duration-300">
                    <span class="material-symbols-outlined text-[20px]">book_online</span>
                </div>
            </div>
            <div class="flex items-center gap-2">
                <span class="flex items-center text-label-sm font-label-sm text-[#0f7a0f] bg-[#e6f4e6] px-2 py-0.5 rounded">
                    <span class="material-symbols-outlined text-[14px] mr-1">trending_up</span> +5.2%
                </span>
                <span class="text-label-sm font-label-sm text-on-surface-variant">vs last month</span>
            </div>
        </div>
        <div class="glass-panel rounded-lg p-stack-md ambient-shadow-lvl1 flex flex-col justify-between group hover:border-outline transition-all duration-300">
            <div class="flex justify-between items-start mb-stack-md">
                <div>
                    <p class="text-label-md font-label-md text-on-surface-variant">Tasa de ocupación</p>
                    <h3 class="text-headline-md font-headline-md text-primary mt-1">@(Model.Dashboard?.OccupancyRate.ToString("0.0") ?? "0.0")%</h3>
                </div>
                <div class="w-10 h-10 rounded-full bg-surface-container flex items-center justify-center text-primary group-hover:bg-primary-fixed transition-all duration-300">
                    <span class="material-symbols-outlined text-[20px]">hotel</span>
                </div>
            </div>
            <div class="flex items-center gap-2">
                <span class="flex items-center text-label-sm font-label-sm text-[#ba1a1a] bg-error-container px-2 py-0.5 rounded">
                    <span class="material-symbols-outlined text-[14px] mr-1">trending_down</span> -2.1%
                </span>
                <span class="text-label-sm font-label-sm text-on-surface-variant">vs last month</span>
            </div>
        </div>
    </section>

    <div class="grid grid-cols-1 lg:grid-cols-3 gap-gutter flex-1">
        <section class="lg:col-span-2 glass-panel rounded-lg p-stack-md ambient-shadow-lvl1 flex flex-col">
            <div class="flex justify-between items-center mb-stack-md">
                <h2 class="text-body-lg font-body-lg font-semibold text-primary">Resumen de ingresos</h2>
                <button class="p-2 text-on-surface-variant hover:text-primary rounded-md hover:bg-surface-container-low transition-all duration-300" type="button">
                    <span class="material-symbols-outlined text-[20px]">more_vert</span>
                </button>
            </div>
            <div class="flex-1 w-full relative min-h-[300px]">
                <canvas id="incomeChart"></canvas>
            </div>
        </section>

        <section class="glass-panel rounded-lg p-0 ambient-shadow-lvl1 flex flex-col overflow-hidden">
            <div class="p-stack-md border-b border-outline-variant/30 flex justify-between items-center">
                <h2 class="text-body-lg font-body-lg font-semibold text-primary">Reservas recientes</h2>
                <a class="text-label-sm font-label-sm text-secondary hover:text-primary transition-all duration-300 underline" href="#">Ver todo</a>
            </div>
            <div class="overflow-y-auto flex-1 max-h-[400px]">
                <table class="w-full min-w-[520px] text-left border-collapse">
                    <tbody>
                        @if (Model.Dashboard?.PropertyMetrics is not null && Model.Dashboard.PropertyMetrics.Any())
                        {
                            foreach (var metric in Model.Dashboard.PropertyMetrics)
                            {
                                <tr class="border-b border-outline-variant/20 hover:bg-surface-container-lowest transition-all duration-300">
                                    <td class="p-4">
                                        <div class="flex items-center gap-3">
                                            <div class="w-8 h-8 rounded-full bg-surface-container-high flex items-center justify-center text-primary font-bold text-label-sm">@metric.Title[..1]</div>
                                            <div>
                                                <p class="text-label-md font-label-md text-primary">@metric.Title</p>
                                                <p class="text-label-sm font-label-sm text-on-surface-variant">@metric.Bookings bookings</p>
                                            </div>
                                        </div>
                                    </td>
                                    <td class="p-4 text-right">
                                        <p class="text-label-md font-label-md text-primary">@metric.Revenue.ToString("C0")</p>
                                        <p class="text-label-sm font-label-sm text-outline">@metric.OccupancyRate.ToString("0.0")%</p>
                                    </td>
                                </tr>
                            }
                        }
                    </tbody>
                </table>
            </div>
        </section>
    </div>
</main>

<script>
    document.addEventListener('DOMContentLoaded', function () {
        const ctx = document.getElementById('incomeChart')?.getContext('2d');
        if (ctx) {
            ctx.fillStyle = '#f2ca50';
        }
    });
</script>
